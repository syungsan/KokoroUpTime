<UserControl x:Class="KokoroUpTimeLogReader.Views.ReplayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KokoroUpTimeLogReader.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        
        <TextBlock x:Name="CurrentSceneText" Text="{Binding CurrentScene}"/>
        <Button VerticalAlignment="Top" HorizontalAlignment="Right" Height="50" Width="100" Command="{Binding BackCommand}" Content="Back"/>
        <TextBlock Text="{Binding ReplayText}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</UserControl>
